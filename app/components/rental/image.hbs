<button
  type="button"
  class="image {{if this.isLarge 'large' 'small'}}"
  {{on "click" this.toggleSize}}
>
  <img ...attributes />
  <small>View {{if this.isLarge "Small" "Large"}}</small>
</button>